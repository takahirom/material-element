<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/few_element_share_card"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:transitionName="@string/transition_name_few_element_share">


    <ImageView
        android:id="@+id/few_element_share_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="20dp"
        android:contentDescription="@null"
        android:transitionName="@string/transition_name_few_element_share_image" />


    <TextView
        android:id="@+id/few_element_not_share_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/few_element_share_image"
        android:layout_toRightOf="@id/few_element_share_image"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Few content elements are shared"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/few_element_share_image"
        android:background="@android:drawable/divider_horizontal_bright" />

    <TextView
        android:id="@+id/all_element_share_no_share_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/divider"
        android:layout_margin="@dimen/spacing_normal_8dp"
        android:layout_marginLeft="@dimen/spacing_normal_8dp"
        android:text="While expanding a surface, if only a single element will be present after the transition, that element should be the focal point of the transition, controlling all other elements." />
</RelativeLayout>